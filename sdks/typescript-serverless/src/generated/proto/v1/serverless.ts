// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.12.0
//   protoc               v3.19.1
// source: v1/serverless.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { AssignedAction } from "../dispatcher";
import { DurableTaskRequest, DurableTaskResponse } from "./dispatcher";
import { CreateWorkflowVersionRequest } from "./workflows";

export const protobufPackage = "v1";

/**
 * ServerlessHealthcheckRequest is the body of POST healthcheck_url. The namespace lets the
 * endpoint apply the same prefix the operator applies to everything it registers.
 */
export interface ServerlessHealthcheckRequest {
  endpointId: string;
  namespace: string;
  /**
   * Unix seconds at which the request was built; the same value is sent in
   * X-Hatchet-Timestamp.
   */
  timestamp: number;
}

/** ServerlessHealthcheckResponse is what an endpoint answers to a healthcheck. */
export interface ServerlessHealthcheckResponse {
  /**
   * The workflow definitions this endpoint serves, without the namespace prefix. The
   * operator namespaces them, derives their action ids and registers them when the
   * canonical form changes.
   */
  workflows: CreateWorkflowVersionRequest[];
  /**
   * Action ids this endpoint serves in addition to the ones derived from workflows, without
   * the namespace prefix. When empty, the endpoint serves exactly the derived actions.
   */
  actions: string[];
  durable: ServerlessDurableSupport | undefined;
  runtime: ServerlessRuntime | undefined;
}

/**
 * ServerlessDurableSupport says whether the endpoint accepts the durable websocket protocol.
 * An endpoint that does not is never assigned durable tasks.
 */
export interface ServerlessDurableSupport {
  supported: boolean;
}

/** ServerlessRuntime identifies the endpoint's runtime and SDK for display on the endpoint. */
export interface ServerlessRuntime {
  /** The platform, such as "cloudflare-workers" or "vercel". */
  name: string;
  sdkVersion: string;
}

/**
 * ServerlessTriggerRequest is the body of POST trigger_url for a non-durable task. The action
 * carries the action id and workflow name as registered, namespace prefix included; the
 * endpoint strips the prefix to dispatch to user code.
 */
export interface ServerlessTriggerRequest {
  endpointId: string;
  namespace: string;
  action:
    | AssignedAction
    | undefined;
  /**
   * Unix seconds at which the request was built; the same value is sent in
   * X-Hatchet-Timestamp.
   */
  timestamp: number;
  /** The envelope version, currently 1. */
  version: number;
}

/**
 * ServerlessTriggerError is the optional body of a non-2xx trigger response. It overrides the
 * status mapping: error replaces the reported message and retry, when set, decides whether
 * the engine retries the task.
 */
export interface ServerlessTriggerError {
  error: string;
  retry?: boolean | undefined;
}

/**
 * ServerlessFirstFrame is the first frame the operator sends on a durable websocket, right
 * after the upgrade.
 */
export interface ServerlessFirstFrame {
  /**
   * The assigned action, namespaced as registered. Its task run external id is the durable
   * task id every request on this socket is stamped with.
   */
  action: AssignedAction | undefined;
  namespace: string;
  /**
   * The invocation this socket serves. The endpoint re-executes the task from the top on
   * every invocation and the engine replays memo, wait_for and trigger_runs from its log.
   */
  invocationCount: number;
  /**
   * How long the endpoint may wait inline for a wait_for entry to complete before it
   * evicts the invocation.
   */
  inlineWaitBudgetMs: number;
}

/**
 * ServerlessErrorFrame reports an engine-side error for the invocation. The endpoint is
 * expected to finish with a done frame carrying the error and retry false.
 */
export interface ServerlessErrorFrame {
  /** "nondeterminism" when a replay diverged from the log, "unspecified" otherwise. */
  code: string;
  message: string;
}

/**
 * ServerlessDoneFrame is the endpoint's terminal frame. Exactly one shape applies, checked in
 * this order: status "evicted" (no terminal event; the engine re-invokes the task when the
 * awaited entry is satisfied), error set (FAILED; retry decides whether the engine retries),
 * otherwise output (COMPLETED; an absent output completes with {}).
 */
export interface ServerlessDoneFrame {
  /**
   * The task's output as a JSON document. It is a string rather than a google.protobuf
   * value so any JSON value is allowed and the operator hands it to the engine byte for
   * byte, without the float64 round trip that Struct and Value impose on numbers.
   */
  output?: string | undefined;
  error?:
    | string
    | undefined;
  /** "evicted" when the invocation evicted itself, empty otherwise. */
  status: string;
  retry: boolean;
}

/**
 * ServerlessDurableFrame is one text frame on the durable websocket. The operator sends
 * first, response and error; the endpoint sends request and done.
 */
export interface ServerlessDurableFrame {
  first?: ServerlessFirstFrame | undefined;
  request?: DurableTaskRequest | undefined;
  response?: DurableTaskResponse | undefined;
  error?: ServerlessErrorFrame | undefined;
  done?:
    | ServerlessDoneFrame
    | undefined;
  /** An endpoint-chosen sequence id on request frames, echoed in the operator's logs only. */
  id?: number | undefined;
}

function createBaseServerlessHealthcheckRequest(): ServerlessHealthcheckRequest {
  return { endpointId: "", namespace: "", timestamp: 0 };
}

export const ServerlessHealthcheckRequest: MessageFns<ServerlessHealthcheckRequest> = {
  encode(message: ServerlessHealthcheckRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.endpointId !== "") {
      writer.uint32(10).string(message.endpointId);
    }
    if (message.namespace !== "") {
      writer.uint32(18).string(message.namespace);
    }
    if (message.timestamp !== 0) {
      writer.uint32(24).int64(message.timestamp);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ServerlessHealthcheckRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseServerlessHealthcheckRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.endpointId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.namespace = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.timestamp = longToNumber(reader.int64());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ServerlessHealthcheckRequest {
    return {
      endpointId: isSet(object.endpointId)
        ? globalThis.String(object.endpointId)
        : isSet(object.endpoint_id)
        ? globalThis.String(object.endpoint_id)
        : "",
      namespace: isSet(object.namespace) ? globalThis.String(object.namespace) : "",
      timestamp: isSet(object.timestamp) ? globalThis.Number(object.timestamp) : 0,
    };
  },

  toJSON(message: ServerlessHealthcheckRequest): unknown {
    const obj: any = {};
    if (message.endpointId !== "") {
      obj.endpointId = message.endpointId;
    }
    if (message.namespace !== "") {
      obj.namespace = message.namespace;
    }
    if (message.timestamp !== 0) {
      obj.timestamp = Math.round(message.timestamp);
    }
    return obj;
  },

  create(base?: DeepPartial<ServerlessHealthcheckRequest>): ServerlessHealthcheckRequest {
    return ServerlessHealthcheckRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<ServerlessHealthcheckRequest>): ServerlessHealthcheckRequest {
    const message = createBaseServerlessHealthcheckRequest();
    message.endpointId = object.endpointId ?? "";
    message.namespace = object.namespace ?? "";
    message.timestamp = object.timestamp ?? 0;
    return message;
  },
};

function createBaseServerlessHealthcheckResponse(): ServerlessHealthcheckResponse {
  return { workflows: [], actions: [], durable: undefined, runtime: undefined };
}

export const ServerlessHealthcheckResponse: MessageFns<ServerlessHealthcheckResponse> = {
  encode(message: ServerlessHealthcheckResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.workflows) {
      CreateWorkflowVersionRequest.encode(v!, writer.uint32(10).fork()).join();
    }
    for (const v of message.actions) {
      writer.uint32(18).string(v!);
    }
    if (message.durable !== undefined) {
      ServerlessDurableSupport.encode(message.durable, writer.uint32(26).fork()).join();
    }
    if (message.runtime !== undefined) {
      ServerlessRuntime.encode(message.runtime, writer.uint32(34).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ServerlessHealthcheckResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseServerlessHealthcheckResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.workflows.push(CreateWorkflowVersionRequest.decode(reader, reader.uint32()));
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.actions.push(reader.string());
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.durable = ServerlessDurableSupport.decode(reader, reader.uint32());
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.runtime = ServerlessRuntime.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ServerlessHealthcheckResponse {
    return {
      workflows: globalThis.Array.isArray(object?.workflows)
        ? object.workflows.map((e: any) => CreateWorkflowVersionRequest.fromJSON(e))
        : [],
      actions: globalThis.Array.isArray(object?.actions) ? object.actions.map((e: any) => globalThis.String(e)) : [],
      durable: isSet(object.durable) ? ServerlessDurableSupport.fromJSON(object.durable) : undefined,
      runtime: isSet(object.runtime) ? ServerlessRuntime.fromJSON(object.runtime) : undefined,
    };
  },

  toJSON(message: ServerlessHealthcheckResponse): unknown {
    const obj: any = {};
    if (message.workflows?.length) {
      obj.workflows = message.workflows.map((e) => CreateWorkflowVersionRequest.toJSON(e));
    }
    if (message.actions?.length) {
      obj.actions = message.actions;
    }
    if (message.durable !== undefined) {
      obj.durable = ServerlessDurableSupport.toJSON(message.durable);
    }
    if (message.runtime !== undefined) {
      obj.runtime = ServerlessRuntime.toJSON(message.runtime);
    }
    return obj;
  },

  create(base?: DeepPartial<ServerlessHealthcheckResponse>): ServerlessHealthcheckResponse {
    return ServerlessHealthcheckResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<ServerlessHealthcheckResponse>): ServerlessHealthcheckResponse {
    const message = createBaseServerlessHealthcheckResponse();
    message.workflows = object.workflows?.map((e) => CreateWorkflowVersionRequest.fromPartial(e)) || [];
    message.actions = object.actions?.map((e) => e) || [];
    message.durable = (object.durable !== undefined && object.durable !== null)
      ? ServerlessDurableSupport.fromPartial(object.durable)
      : undefined;
    message.runtime = (object.runtime !== undefined && object.runtime !== null)
      ? ServerlessRuntime.fromPartial(object.runtime)
      : undefined;
    return message;
  },
};

function createBaseServerlessDurableSupport(): ServerlessDurableSupport {
  return { supported: false };
}

export const ServerlessDurableSupport: MessageFns<ServerlessDurableSupport> = {
  encode(message: ServerlessDurableSupport, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.supported !== false) {
      writer.uint32(8).bool(message.supported);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ServerlessDurableSupport {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseServerlessDurableSupport();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.supported = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ServerlessDurableSupport {
    return { supported: isSet(object.supported) ? globalThis.Boolean(object.supported) : false };
  },

  toJSON(message: ServerlessDurableSupport): unknown {
    const obj: any = {};
    if (message.supported !== false) {
      obj.supported = message.supported;
    }
    return obj;
  },

  create(base?: DeepPartial<ServerlessDurableSupport>): ServerlessDurableSupport {
    return ServerlessDurableSupport.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<ServerlessDurableSupport>): ServerlessDurableSupport {
    const message = createBaseServerlessDurableSupport();
    message.supported = object.supported ?? false;
    return message;
  },
};

function createBaseServerlessRuntime(): ServerlessRuntime {
  return { name: "", sdkVersion: "" };
}

export const ServerlessRuntime: MessageFns<ServerlessRuntime> = {
  encode(message: ServerlessRuntime, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.name !== "") {
      writer.uint32(10).string(message.name);
    }
    if (message.sdkVersion !== "") {
      writer.uint32(18).string(message.sdkVersion);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ServerlessRuntime {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseServerlessRuntime();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.name = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.sdkVersion = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ServerlessRuntime {
    return {
      name: isSet(object.name) ? globalThis.String(object.name) : "",
      sdkVersion: isSet(object.sdkVersion)
        ? globalThis.String(object.sdkVersion)
        : isSet(object.sdk_version)
        ? globalThis.String(object.sdk_version)
        : "",
    };
  },

  toJSON(message: ServerlessRuntime): unknown {
    const obj: any = {};
    if (message.name !== "") {
      obj.name = message.name;
    }
    if (message.sdkVersion !== "") {
      obj.sdkVersion = message.sdkVersion;
    }
    return obj;
  },

  create(base?: DeepPartial<ServerlessRuntime>): ServerlessRuntime {
    return ServerlessRuntime.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<ServerlessRuntime>): ServerlessRuntime {
    const message = createBaseServerlessRuntime();
    message.name = object.name ?? "";
    message.sdkVersion = object.sdkVersion ?? "";
    return message;
  },
};

function createBaseServerlessTriggerRequest(): ServerlessTriggerRequest {
  return { endpointId: "", namespace: "", action: undefined, timestamp: 0, version: 0 };
}

export const ServerlessTriggerRequest: MessageFns<ServerlessTriggerRequest> = {
  encode(message: ServerlessTriggerRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.endpointId !== "") {
      writer.uint32(10).string(message.endpointId);
    }
    if (message.namespace !== "") {
      writer.uint32(18).string(message.namespace);
    }
    if (message.action !== undefined) {
      AssignedAction.encode(message.action, writer.uint32(26).fork()).join();
    }
    if (message.timestamp !== 0) {
      writer.uint32(32).int64(message.timestamp);
    }
    if (message.version !== 0) {
      writer.uint32(40).int32(message.version);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ServerlessTriggerRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseServerlessTriggerRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.endpointId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.namespace = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.action = AssignedAction.decode(reader, reader.uint32());
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.timestamp = longToNumber(reader.int64());
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.version = reader.int32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ServerlessTriggerRequest {
    return {
      endpointId: isSet(object.endpointId)
        ? globalThis.String(object.endpointId)
        : isSet(object.endpoint_id)
        ? globalThis.String(object.endpoint_id)
        : "",
      namespace: isSet(object.namespace) ? globalThis.String(object.namespace) : "",
      action: isSet(object.action) ? AssignedAction.fromJSON(object.action) : undefined,
      timestamp: isSet(object.timestamp) ? globalThis.Number(object.timestamp) : 0,
      version: isSet(object.version) ? globalThis.Number(object.version) : 0,
    };
  },

  toJSON(message: ServerlessTriggerRequest): unknown {
    const obj: any = {};
    if (message.endpointId !== "") {
      obj.endpointId = message.endpointId;
    }
    if (message.namespace !== "") {
      obj.namespace = message.namespace;
    }
    if (message.action !== undefined) {
      obj.action = AssignedAction.toJSON(message.action);
    }
    if (message.timestamp !== 0) {
      obj.timestamp = Math.round(message.timestamp);
    }
    if (message.version !== 0) {
      obj.version = Math.round(message.version);
    }
    return obj;
  },

  create(base?: DeepPartial<ServerlessTriggerRequest>): ServerlessTriggerRequest {
    return ServerlessTriggerRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<ServerlessTriggerRequest>): ServerlessTriggerRequest {
    const message = createBaseServerlessTriggerRequest();
    message.endpointId = object.endpointId ?? "";
    message.namespace = object.namespace ?? "";
    message.action = (object.action !== undefined && object.action !== null)
      ? AssignedAction.fromPartial(object.action)
      : undefined;
    message.timestamp = object.timestamp ?? 0;
    message.version = object.version ?? 0;
    return message;
  },
};

function createBaseServerlessTriggerError(): ServerlessTriggerError {
  return { error: "", retry: undefined };
}

export const ServerlessTriggerError: MessageFns<ServerlessTriggerError> = {
  encode(message: ServerlessTriggerError, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.error !== "") {
      writer.uint32(10).string(message.error);
    }
    if (message.retry !== undefined) {
      writer.uint32(16).bool(message.retry);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ServerlessTriggerError {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseServerlessTriggerError();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.error = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.retry = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ServerlessTriggerError {
    return {
      error: isSet(object.error) ? globalThis.String(object.error) : "",
      retry: isSet(object.retry) ? globalThis.Boolean(object.retry) : undefined,
    };
  },

  toJSON(message: ServerlessTriggerError): unknown {
    const obj: any = {};
    if (message.error !== "") {
      obj.error = message.error;
    }
    if (message.retry !== undefined) {
      obj.retry = message.retry;
    }
    return obj;
  },

  create(base?: DeepPartial<ServerlessTriggerError>): ServerlessTriggerError {
    return ServerlessTriggerError.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<ServerlessTriggerError>): ServerlessTriggerError {
    const message = createBaseServerlessTriggerError();
    message.error = object.error ?? "";
    message.retry = object.retry ?? undefined;
    return message;
  },
};

function createBaseServerlessFirstFrame(): ServerlessFirstFrame {
  return { action: undefined, namespace: "", invocationCount: 0, inlineWaitBudgetMs: 0 };
}

export const ServerlessFirstFrame: MessageFns<ServerlessFirstFrame> = {
  encode(message: ServerlessFirstFrame, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.action !== undefined) {
      AssignedAction.encode(message.action, writer.uint32(10).fork()).join();
    }
    if (message.namespace !== "") {
      writer.uint32(18).string(message.namespace);
    }
    if (message.invocationCount !== 0) {
      writer.uint32(24).int32(message.invocationCount);
    }
    if (message.inlineWaitBudgetMs !== 0) {
      writer.uint32(32).int32(message.inlineWaitBudgetMs);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ServerlessFirstFrame {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseServerlessFirstFrame();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.action = AssignedAction.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.namespace = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.invocationCount = reader.int32();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.inlineWaitBudgetMs = reader.int32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ServerlessFirstFrame {
    return {
      action: isSet(object.action) ? AssignedAction.fromJSON(object.action) : undefined,
      namespace: isSet(object.namespace) ? globalThis.String(object.namespace) : "",
      invocationCount: isSet(object.invocationCount)
        ? globalThis.Number(object.invocationCount)
        : isSet(object.invocation_count)
        ? globalThis.Number(object.invocation_count)
        : 0,
      inlineWaitBudgetMs: isSet(object.inlineWaitBudgetMs)
        ? globalThis.Number(object.inlineWaitBudgetMs)
        : isSet(object.inline_wait_budget_ms)
        ? globalThis.Number(object.inline_wait_budget_ms)
        : 0,
    };
  },

  toJSON(message: ServerlessFirstFrame): unknown {
    const obj: any = {};
    if (message.action !== undefined) {
      obj.action = AssignedAction.toJSON(message.action);
    }
    if (message.namespace !== "") {
      obj.namespace = message.namespace;
    }
    if (message.invocationCount !== 0) {
      obj.invocationCount = Math.round(message.invocationCount);
    }
    if (message.inlineWaitBudgetMs !== 0) {
      obj.inlineWaitBudgetMs = Math.round(message.inlineWaitBudgetMs);
    }
    return obj;
  },

  create(base?: DeepPartial<ServerlessFirstFrame>): ServerlessFirstFrame {
    return ServerlessFirstFrame.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<ServerlessFirstFrame>): ServerlessFirstFrame {
    const message = createBaseServerlessFirstFrame();
    message.action = (object.action !== undefined && object.action !== null)
      ? AssignedAction.fromPartial(object.action)
      : undefined;
    message.namespace = object.namespace ?? "";
    message.invocationCount = object.invocationCount ?? 0;
    message.inlineWaitBudgetMs = object.inlineWaitBudgetMs ?? 0;
    return message;
  },
};

function createBaseServerlessErrorFrame(): ServerlessErrorFrame {
  return { code: "", message: "" };
}

export const ServerlessErrorFrame: MessageFns<ServerlessErrorFrame> = {
  encode(message: ServerlessErrorFrame, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.code !== "") {
      writer.uint32(10).string(message.code);
    }
    if (message.message !== "") {
      writer.uint32(18).string(message.message);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ServerlessErrorFrame {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseServerlessErrorFrame();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.code = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.message = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ServerlessErrorFrame {
    return {
      code: isSet(object.code) ? globalThis.String(object.code) : "",
      message: isSet(object.message) ? globalThis.String(object.message) : "",
    };
  },

  toJSON(message: ServerlessErrorFrame): unknown {
    const obj: any = {};
    if (message.code !== "") {
      obj.code = message.code;
    }
    if (message.message !== "") {
      obj.message = message.message;
    }
    return obj;
  },

  create(base?: DeepPartial<ServerlessErrorFrame>): ServerlessErrorFrame {
    return ServerlessErrorFrame.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<ServerlessErrorFrame>): ServerlessErrorFrame {
    const message = createBaseServerlessErrorFrame();
    message.code = object.code ?? "";
    message.message = object.message ?? "";
    return message;
  },
};

function createBaseServerlessDoneFrame(): ServerlessDoneFrame {
  return { output: undefined, error: undefined, status: "", retry: false };
}

export const ServerlessDoneFrame: MessageFns<ServerlessDoneFrame> = {
  encode(message: ServerlessDoneFrame, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.output !== undefined) {
      writer.uint32(10).string(message.output);
    }
    if (message.error !== undefined) {
      writer.uint32(18).string(message.error);
    }
    if (message.status !== "") {
      writer.uint32(26).string(message.status);
    }
    if (message.retry !== false) {
      writer.uint32(32).bool(message.retry);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ServerlessDoneFrame {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseServerlessDoneFrame();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.output = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.error = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.status = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.retry = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ServerlessDoneFrame {
    return {
      output: isSet(object.output) ? globalThis.String(object.output) : undefined,
      error: isSet(object.error) ? globalThis.String(object.error) : undefined,
      status: isSet(object.status) ? globalThis.String(object.status) : "",
      retry: isSet(object.retry) ? globalThis.Boolean(object.retry) : false,
    };
  },

  toJSON(message: ServerlessDoneFrame): unknown {
    const obj: any = {};
    if (message.output !== undefined) {
      obj.output = message.output;
    }
    if (message.error !== undefined) {
      obj.error = message.error;
    }
    if (message.status !== "") {
      obj.status = message.status;
    }
    if (message.retry !== false) {
      obj.retry = message.retry;
    }
    return obj;
  },

  create(base?: DeepPartial<ServerlessDoneFrame>): ServerlessDoneFrame {
    return ServerlessDoneFrame.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<ServerlessDoneFrame>): ServerlessDoneFrame {
    const message = createBaseServerlessDoneFrame();
    message.output = object.output ?? undefined;
    message.error = object.error ?? undefined;
    message.status = object.status ?? "";
    message.retry = object.retry ?? false;
    return message;
  },
};

function createBaseServerlessDurableFrame(): ServerlessDurableFrame {
  return {
    first: undefined,
    request: undefined,
    response: undefined,
    error: undefined,
    done: undefined,
    id: undefined,
  };
}

export const ServerlessDurableFrame: MessageFns<ServerlessDurableFrame> = {
  encode(message: ServerlessDurableFrame, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.first !== undefined) {
      ServerlessFirstFrame.encode(message.first, writer.uint32(10).fork()).join();
    }
    if (message.request !== undefined) {
      DurableTaskRequest.encode(message.request, writer.uint32(18).fork()).join();
    }
    if (message.response !== undefined) {
      DurableTaskResponse.encode(message.response, writer.uint32(26).fork()).join();
    }
    if (message.error !== undefined) {
      ServerlessErrorFrame.encode(message.error, writer.uint32(34).fork()).join();
    }
    if (message.done !== undefined) {
      ServerlessDoneFrame.encode(message.done, writer.uint32(42).fork()).join();
    }
    if (message.id !== undefined) {
      writer.uint32(48).int64(message.id);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ServerlessDurableFrame {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseServerlessDurableFrame();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.first = ServerlessFirstFrame.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.request = DurableTaskRequest.decode(reader, reader.uint32());
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.response = DurableTaskResponse.decode(reader, reader.uint32());
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.error = ServerlessErrorFrame.decode(reader, reader.uint32());
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.done = ServerlessDoneFrame.decode(reader, reader.uint32());
          continue;
        }
        case 6: {
          if (tag !== 48) {
            break;
          }

          message.id = longToNumber(reader.int64());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ServerlessDurableFrame {
    return {
      first: isSet(object.first) ? ServerlessFirstFrame.fromJSON(object.first) : undefined,
      request: isSet(object.request) ? DurableTaskRequest.fromJSON(object.request) : undefined,
      response: isSet(object.response) ? DurableTaskResponse.fromJSON(object.response) : undefined,
      error: isSet(object.error) ? ServerlessErrorFrame.fromJSON(object.error) : undefined,
      done: isSet(object.done) ? ServerlessDoneFrame.fromJSON(object.done) : undefined,
      id: isSet(object.id) ? globalThis.Number(object.id) : undefined,
    };
  },

  toJSON(message: ServerlessDurableFrame): unknown {
    const obj: any = {};
    if (message.first !== undefined) {
      obj.first = ServerlessFirstFrame.toJSON(message.first);
    }
    if (message.request !== undefined) {
      obj.request = DurableTaskRequest.toJSON(message.request);
    }
    if (message.response !== undefined) {
      obj.response = DurableTaskResponse.toJSON(message.response);
    }
    if (message.error !== undefined) {
      obj.error = ServerlessErrorFrame.toJSON(message.error);
    }
    if (message.done !== undefined) {
      obj.done = ServerlessDoneFrame.toJSON(message.done);
    }
    if (message.id !== undefined) {
      obj.id = Math.round(message.id);
    }
    return obj;
  },

  create(base?: DeepPartial<ServerlessDurableFrame>): ServerlessDurableFrame {
    return ServerlessDurableFrame.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<ServerlessDurableFrame>): ServerlessDurableFrame {
    const message = createBaseServerlessDurableFrame();
    message.first = (object.first !== undefined && object.first !== null)
      ? ServerlessFirstFrame.fromPartial(object.first)
      : undefined;
    message.request = (object.request !== undefined && object.request !== null)
      ? DurableTaskRequest.fromPartial(object.request)
      : undefined;
    message.response = (object.response !== undefined && object.response !== null)
      ? DurableTaskResponse.fromPartial(object.response)
      : undefined;
    message.error = (object.error !== undefined && object.error !== null)
      ? ServerlessErrorFrame.fromPartial(object.error)
      : undefined;
    message.done = (object.done !== undefined && object.done !== null)
      ? ServerlessDoneFrame.fromPartial(object.done)
      : undefined;
    message.id = object.id ?? undefined;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

function longToNumber(int64: { toString(): string }): number {
  const num = globalThis.Number(int64.toString());
  if (num > globalThis.Number.MAX_SAFE_INTEGER) {
    throw new globalThis.Error("Value is larger than Number.MAX_SAFE_INTEGER");
  }
  if (num < globalThis.Number.MIN_SAFE_INTEGER) {
    throw new globalThis.Error("Value is smaller than Number.MIN_SAFE_INTEGER");
  }
  return num;
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create(base?: DeepPartial<T>): T;
  fromPartial(object: DeepPartial<T>): T;
}
