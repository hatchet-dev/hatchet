# coding: utf-8

"""
Hatchet API

The Hatchet API

The version of the OpenAPI document: 1.0.0
Generated by OpenAPI Generator (https://openapi-generator.tech)

Do not edit the class manually.
"""  # noqa: E501

from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictBool, StrictInt, StrictStr
from typing import Any, ClassVar, Dict, List
from hatchet_sdk.clients.rest.models.api_resource_meta import APIResourceMeta
from hatchet_sdk.clients.rest.models.v1_serverless_endpoint_kind import (
    V1ServerlessEndpointKind,
)
from hatchet_sdk.clients.rest.models.v1_serverless_endpoint_status import (
    V1ServerlessEndpointStatus,
)
from typing import Optional, Set
from typing_extensions import Self


class V1ServerlessEndpoint(BaseModel):
    """
    V1ServerlessEndpoint
    """  # noqa: E501

    metadata: APIResourceMeta
    tenant_id: StrictStr = Field(
        description="The ID of the tenant that owns this endpoint.", alias="tenantId"
    )
    name: StrictStr = Field(
        description="The name of the endpoint. Unique within the tenant."
    )
    namespace: StrictStr = Field(
        description='The prefix applied to everything this endpoint registers (workflows, actions, events), as "<namespace>_". Read-only: assigned on creation and immutable.'
    )
    kind: V1ServerlessEndpointKind
    healthcheck_url: StrictStr = Field(
        description="The HTTPS URL (port 443) polled periodically to discover the workflows this endpoint serves.",
        alias="healthcheckUrl",
    )
    trigger_url: StrictStr = Field(
        description="The HTTPS URL (port 443) that assigned tasks are delivered to.",
        alias="triggerUrl",
    )
    request_timeout_seconds: StrictInt = Field(
        description="The per-request timeout backstop for trigger requests, in seconds.",
        alias="requestTimeoutSeconds",
    )
    poll_interval_seconds: StrictInt = Field(
        description="How often the healthcheck URL is polled, in seconds.",
        alias="pollIntervalSeconds",
    )
    inline_wait_budget_ms: StrictInt = Field(
        description="How long a trigger request may block waiting for a durable result before the operator falls back to the websocket durable protocol, in milliseconds.",
        alias="inlineWaitBudgetMs",
    )
    labels: Dict[str, Any] = Field(
        description="Worker labels applied to the connection-backed worker the operator creates for this endpoint."
    )
    enabled: StrictBool = Field(
        description="Whether the operator polls and dispatches to this endpoint."
    )
    status: V1ServerlessEndpointStatus
    __properties: ClassVar[List[str]] = [
        "metadata",
        "tenantId",
        "name",
        "namespace",
        "kind",
        "healthcheckUrl",
        "triggerUrl",
        "requestTimeoutSeconds",
        "pollIntervalSeconds",
        "inlineWaitBudgetMs",
        "labels",
        "enabled",
        "status",
    ]

    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )

    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of V1ServerlessEndpoint from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        # override the default output from pydantic by calling `to_dict()` of metadata
        if self.metadata:
            _dict["metadata"] = self.metadata.to_dict()
        # override the default output from pydantic by calling `to_dict()` of status
        if self.status:
            _dict["status"] = self.status.to_dict()
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of V1ServerlessEndpoint from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate(
            {
                "metadata": (
                    APIResourceMeta.from_dict(obj["metadata"])
                    if obj.get("metadata") is not None
                    else None
                ),
                "tenantId": obj.get("tenantId"),
                "name": obj.get("name"),
                "namespace": obj.get("namespace"),
                "kind": obj.get("kind"),
                "healthcheckUrl": obj.get("healthcheckUrl"),
                "triggerUrl": obj.get("triggerUrl"),
                "requestTimeoutSeconds": obj.get("requestTimeoutSeconds"),
                "pollIntervalSeconds": obj.get("pollIntervalSeconds"),
                "inlineWaitBudgetMs": obj.get("inlineWaitBudgetMs"),
                "labels": obj.get("labels"),
                "enabled": obj.get("enabled"),
                "status": (
                    V1ServerlessEndpointStatus.from_dict(obj["status"])
                    if obj.get("status") is not None
                    else None
                ),
            }
        )
        return _obj
