# coding: utf-8

"""
Hatchet API

The Hatchet API

The version of the OpenAPI document: 1.0.0
Generated by OpenAPI Generator (https://openapi-generator.tech)

Do not edit the class manually.
"""  # noqa: E501

from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictBool, StrictInt, StrictStr
from typing import Any, ClassVar, Dict, List, Optional
from typing_extensions import Annotated
from hatchet_sdk.clients.rest.models.v1_serverless_endpoint_kind import (
    V1ServerlessEndpointKind,
)
from typing import Optional, Set
from typing_extensions import Self


class V1CreateServerlessEndpointRequest(BaseModel):
    """
    V1CreateServerlessEndpointRequest
    """  # noqa: E501

    name: StrictStr = Field(
        description="The name of the endpoint. Unique within the tenant."
    )
    kind: Optional[V1ServerlessEndpointKind] = None
    healthcheck_url: StrictStr = Field(
        description="The HTTPS URL (port 443) polled periodically to discover the workflows this endpoint serves.",
        alias="healthcheckUrl",
    )
    trigger_url: StrictStr = Field(
        description="The HTTPS URL (port 443) that assigned tasks are delivered to.",
        alias="triggerUrl",
    )
    signing_secret: Annotated[str, Field(min_length=32, strict=True)] = Field(
        description="The secret used to HMAC-sign requests delivered to the endpoint. At least 32 characters. Write-only: it is stored encrypted and never returned in responses.",
        alias="signingSecret",
    )
    request_timeout_seconds: Optional[StrictInt] = Field(
        default=None,
        description="The per-request timeout backstop for trigger requests, in seconds. Defaults to 60.",
        alias="requestTimeoutSeconds",
    )
    poll_interval_seconds: Optional[StrictInt] = Field(
        default=None,
        description="How often the healthcheck URL is polled, in seconds. Defaults to 30.",
        alias="pollIntervalSeconds",
    )
    inline_wait_budget_ms: Optional[StrictInt] = Field(
        default=None,
        description="The inline wait budget for durable results, in milliseconds. Defaults to 5000.",
        alias="inlineWaitBudgetMs",
    )
    labels: Optional[Dict[str, Any]] = Field(
        default=None,
        description="Worker labels applied to the worker the operator creates for this endpoint.",
    )
    enabled: Optional[StrictBool] = Field(
        default=None,
        description="Whether the operator polls and dispatches to this endpoint. Defaults to true.",
    )
    __properties: ClassVar[List[str]] = [
        "name",
        "kind",
        "healthcheckUrl",
        "triggerUrl",
        "signingSecret",
        "requestTimeoutSeconds",
        "pollIntervalSeconds",
        "inlineWaitBudgetMs",
        "labels",
        "enabled",
    ]

    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )

    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of V1CreateServerlessEndpointRequest from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of V1CreateServerlessEndpointRequest from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate(
            {
                "name": obj.get("name"),
                "kind": obj.get("kind"),
                "healthcheckUrl": obj.get("healthcheckUrl"),
                "triggerUrl": obj.get("triggerUrl"),
                "signingSecret": obj.get("signingSecret"),
                "requestTimeoutSeconds": obj.get("requestTimeoutSeconds"),
                "pollIntervalSeconds": obj.get("pollIntervalSeconds"),
                "inlineWaitBudgetMs": obj.get("inlineWaitBudgetMs"),
                "labels": obj.get("labels"),
                "enabled": obj.get("enabled"),
            }
        )
        return _obj
