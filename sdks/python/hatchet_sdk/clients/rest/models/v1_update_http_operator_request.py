# coding: utf-8

"""
Hatchet API

The Hatchet API

The version of the OpenAPI document: 1.0.0
Generated by OpenAPI Generator (https://openapi-generator.tech)

Do not edit the class manually.
"""  # noqa: E501

from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictInt, StrictStr
from typing import Any, ClassVar, Dict, List, Optional
from typing import Optional, Set
from typing_extensions import Self


class V1UpdateHTTPOperatorRequest(BaseModel):
    """
    Fields to update on an HTTP operator. Omitted fields are left unchanged.
    """  # noqa: E501

    trigger_endpoint: Optional[StrictStr] = Field(
        default=None,
        description="The HTTPS endpoint (https, port 443) that assigned tasks are delivered to.",
        alias="triggerEndpoint",
    )
    healthcheck_endpoint: Optional[StrictStr] = Field(
        default=None,
        description="An optional HTTPS endpoint polled to verify the operator endpoint is reachable.",
        alias="healthcheckEndpoint",
    )
    signing_secret: Optional[StrictStr] = Field(
        default=None,
        description="The secret used to HMAC-sign delivered requests. Write-only: it is never returned in responses. Provide a new value to rotate it.",
        alias="signingSecret",
    )
    request_timeout_seconds: Optional[StrictInt] = Field(
        default=None,
        description="Optional override for the per-request timeout backstop, in seconds.",
        alias="requestTimeoutSeconds",
    )
    __properties: ClassVar[List[str]] = [
        "triggerEndpoint",
        "healthcheckEndpoint",
        "signingSecret",
        "requestTimeoutSeconds",
    ]

    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )

    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of V1UpdateHTTPOperatorRequest from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of V1UpdateHTTPOperatorRequest from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate(
            {
                "triggerEndpoint": obj.get("triggerEndpoint"),
                "healthcheckEndpoint": obj.get("healthcheckEndpoint"),
                "signingSecret": obj.get("signingSecret"),
                "requestTimeoutSeconds": obj.get("requestTimeoutSeconds"),
            }
        )
        return _obj
