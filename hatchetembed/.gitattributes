# The dashboard/ directory holds the built frontend, generated by `task dashboard:embed` from
# frontend/app and embedded into this package via go:embed. It is generated output, not source —
# mark it so reviews collapse the diffs, language stats ignore it, and merges don't conflict on it.
dashboard/** linguist-generated=true
dashboard/** -diff
dashboard/** -merge
