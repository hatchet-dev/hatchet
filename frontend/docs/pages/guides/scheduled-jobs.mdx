import { Callout, Cards, Steps, Tabs } from "nextra/components";
import { snippets } from "@/lib/generated/snippets";
import { Snippet } from "@/components/code";
import UniversalTabs from "@/components/UniversalTabs";

# Scheduled Jobs / Cron

Scheduled jobs run recurring or one-off work at specific times: database maintenance, report generation, cache invalidation, or periodic data syncs. Hatchet supports both **cron expressions** for recurring schedules and **scheduled runs** for one-time future execution, all with the same retry, concurrency, and observability features as event-driven tasks.

## Step-by-step walkthrough

By the end of this guide you will have a cron-triggered task and a one-time scheduled run.

<Steps>

### Step 1: Define the cron task

Create a task that runs on a cron schedule. Attach the cron expression to the workflow or task definition.

<UniversalTabs items={["Python", "Typescript", "Go", "Ruby"]}>
  <Tabs.Tab title="Python">
    <Snippet
      src={
        snippets.python.guides.scheduled_jobs.worker.step_01_define_cron_task
      }
    />
  </Tabs.Tab>
  <Tabs.Tab title="Typescript">
    <Snippet
      src={
        snippets.typescript.guides.scheduled_jobs.workflow
          .step_01_define_cron_task
      }
    />
  </Tabs.Tab>
  <Tabs.Tab title="Go">
    <Snippet
      src={snippets.go.guides.scheduled_jobs.main.step_01_define_cron_task}
    />
  </Tabs.Tab>
  <Tabs.Tab title="Ruby">
    <Snippet
      src={snippets.ruby.guides.scheduled_jobs.worker.step_01_define_cron_task}
    />
  </Tabs.Tab>
</UniversalTabs>

### Step 2: Schedule a one-time run

Use the scheduled run API on the workflow from Step 1 to trigger execution at a specific future time.

<UniversalTabs items={["Python", "Typescript", "Go", "Ruby"]} variant="hidden">
  <Tabs.Tab title="Python">
    <Snippet
      src={
        snippets.python.guides.scheduled_jobs.trigger.step_02_schedule_one_time
      }
    />
  </Tabs.Tab>
  <Tabs.Tab title="Typescript">
    <Snippet
      src={
        snippets.typescript.guides.scheduled_jobs.trigger
          .step_02_schedule_one_time
      }
    />
  </Tabs.Tab>
  <Tabs.Tab title="Go">
    <Snippet
      src={snippets.go.guides.scheduled_jobs.trigger.step_02_schedule_one_time}
    />
  </Tabs.Tab>
  <Tabs.Tab title="Ruby">
    <Snippet
      src={
        snippets.ruby.guides.scheduled_jobs.trigger.step_02_schedule_one_time
      }
    />
  </Tabs.Tab>
</UniversalTabs>

### Step 3: Run the worker

Start the worker. The cron schedule is activated when the worker registers with Hatchet.

<UniversalTabs items={["Python", "Typescript", "Go", "Ruby"]} variant="hidden">
  <Tabs.Tab title="Python">
    <Snippet
      src={snippets.python.guides.scheduled_jobs.worker.step_03_run_worker}
    />
  </Tabs.Tab>
  <Tabs.Tab title="Typescript">
    <Snippet
      src={snippets.typescript.guides.scheduled_jobs.worker.step_03_run_worker}
    />
  </Tabs.Tab>
  <Tabs.Tab title="Go">
    <Snippet src={snippets.go.guides.scheduled_jobs.main.step_03_run_worker} />
  </Tabs.Tab>
  <Tabs.Tab title="Ruby">
    <Snippet
      src={snippets.ruby.guides.scheduled_jobs.worker.step_03_run_worker}
    />
  </Tabs.Tab>
</UniversalTabs>

</Steps>

Cron triggers are defined alongside your task code, so there is no separate infrastructure to manage. The schedule activates as soon as your worker starts.

<Callout type="warning">
  Always configure **concurrency controls** for scheduled jobs to prevent
  overlapping runs. A job that runs every 5 minutes but takes 10 minutes to
  complete will cause pileups without concurrency limits.
</Callout>

## Common Patterns

| Pattern                   | Description                                                                |
| ------------------------- | -------------------------------------------------------------------------- |
| **Database maintenance**  | Run cleanup, vacuum, or archival jobs on a schedule                        |
| **Report generation**     | Generate daily/weekly reports and deliver via email or dashboard           |
| **Cache invalidation**    | Periodically refresh caches or materialized views                          |
| **Data sync**             | Pull data from external APIs on a schedule to keep local copies up to date |
| **Health checks**         | Run periodic health checks against dependent services                      |
| **Billing and invoicing** | Generate invoices or process billing events on a recurring schedule        |

## Related Patterns

<Cards>
  <Cards.Card title="Event-Driven Systems" href="/guides/event-driven">
    When work should be triggered by events rather than a fixed schedule.
  </Cards.Card>
  <Cards.Card title="Batch Processing" href="/guides/batch-processing">
    Combine scheduled triggers with batch fan-out for periodic bulk operations.
  </Cards.Card>
  <Cards.Card title="Webhook Processing" href="/guides/webhook-processing">
    Process incoming webhooks, an alternative to polling on a schedule.
  </Cards.Card>
</Cards>

## Next Steps

- [Cron Triggers](/concepts/cron-runs): cron expression syntax and configuration
- [Scheduled Runs](/concepts/scheduled-runs): one-time future execution API
- [Concurrency Control](/concepts/concurrency): prevent overlapping scheduled jobs
- [Retry Policies](/concepts/retry-policies): handle failures in scheduled jobs
