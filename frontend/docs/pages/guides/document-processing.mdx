import { Callout, Cards, Steps, Tabs } from "nextra/components";
import DocumentProcessingDiagram from "@/components/DocumentProcessingDiagramWrapper";
import LLMIntegrationTabs from "@/components/LLMIntegrationTabs.mdx";
import OCRIntegrationTabs from "@/components/OCRIntegrationTabs.mdx";
import { snippets } from "@/lib/generated/snippets";
import { Snippet } from "@/components/code";
import UniversalTabs from "@/components/UniversalTabs";

# Document Processing

PDF and file pipelines are central to AI workflows: parse documents, extract content with OCR or LLMs, validate and enrich the data, and classify or route it. We'll structure these as Hatchet [DAG workflows](/concepts/durable-workflows/directed-acyclic-graphs).

<DocumentProcessingDiagram />

Because the stages are fixed (ingest → parse → extract), document pipelines map naturally to [DAG workflows](/concepts/durable-workflows/directed-acyclic-graphs). You can add [child spawning](/concepts/durable-workflows/directed-acyclic-graphs/child-spawning) within the ingest or parse stage to process multiple documents in parallel.

## Step-by-step walkthrough

You'll build a three-stage pipeline (ingest, parse, extract) using mocks so you can run it locally without API keys.

<Steps>

### Define the DAG

Create a workflow with a fixed pipeline: ingest, parse, extract. Each stage depends on the previous.

<UniversalTabs items={["Python", "Typescript", "Go", "Ruby"]}>
  <Tabs.Tab title="Python">
    <Snippet
      src={snippets.python.guides.document_processing.worker.step_01_define_dag}
    />
  </Tabs.Tab>
  <Tabs.Tab title="Typescript">
    <Snippet
      src={
        snippets.typescript.guides.document_processing.workflow
          .step_01_define_dag
      }
    />
  </Tabs.Tab>
  <Tabs.Tab title="Go">
    <Snippet
      src={snippets.go.guides.document_processing.main.step_01_define_dag}
    />
  </Tabs.Tab>
  <Tabs.Tab title="Ruby">
    <Snippet
      src={snippets.ruby.guides.document_processing.worker.step_01_define_dag}
    />
  </Tabs.Tab>
</UniversalTabs>

### Parse stage

The parse task depends on ingest (Step 1). It converts raw content to structured text (use OCR for images, mock for examples).

<UniversalTabs items={["Python", "Typescript", "Go", "Ruby"]} variant="hidden">
  <Tabs.Tab title="Python">
    <Snippet
      src={
        snippets.python.guides.document_processing.worker.step_02_parse_stage
      }
    />
  </Tabs.Tab>
  <Tabs.Tab title="Typescript">
    <Snippet
      src={
        snippets.typescript.guides.document_processing.workflow
          .step_02_parse_stage
      }
    />
  </Tabs.Tab>
  <Tabs.Tab title="Go">
    <Snippet
      src={snippets.go.guides.document_processing.main.step_02_parse_stage}
    />
  </Tabs.Tab>
  <Tabs.Tab title="Ruby">
    <Snippet
      src={snippets.ruby.guides.document_processing.worker.step_02_parse_stage}
    />
  </Tabs.Tab>
</UniversalTabs>

The examples above use a mock OCR service. To use a real provider, swap in one of these. Pick a provider, then your language:

<OCRIntegrationTabs />

### Extract stage

The extract task depends on parse (Step 2). Use an LLM or rules to extract entities. [Rate Limits](/concepts/rate-limits) keep extract tasks within provider quotas.

<UniversalTabs items={["Python", "Typescript", "Go", "Ruby"]} variant="hidden">
  <Tabs.Tab title="Python">
    <Snippet
      src={
        snippets.python.guides.document_processing.worker.step_03_extract_stage
      }
    />
  </Tabs.Tab>
  <Tabs.Tab title="Typescript">
    <Snippet
      src={
        snippets.typescript.guides.document_processing.workflow
          .step_03_extract_stage
      }
    />
  </Tabs.Tab>
  <Tabs.Tab title="Go">
    <Snippet
      src={snippets.go.guides.document_processing.main.step_03_extract_stage}
    />
  </Tabs.Tab>
  <Tabs.Tab title="Ruby">
    <Snippet
      src={
        snippets.ruby.guides.document_processing.worker.step_03_extract_stage
      }
    />
  </Tabs.Tab>
</UniversalTabs>

The examples above use a mock extractor. To wire in a real LLM for extraction, swap `get_extract_service()` with a provider:

<LLMIntegrationTabs />

### Run the worker

Start the worker. For per-document parallelism, use [Child Spawning](/concepts/durable-workflows/directed-acyclic-graphs/child-spawning) within the ingest stage.

<UniversalTabs items={["Python", "Typescript", "Go", "Ruby"]} variant="hidden">
  <Tabs.Tab title="Python">
    <Snippet
      src={snippets.python.guides.document_processing.worker.step_04_run_worker}
    />
  </Tabs.Tab>
  <Tabs.Tab title="Typescript">
    <Snippet
      src={
        snippets.typescript.guides.document_processing.worker.step_04_run_worker
      }
    />
  </Tabs.Tab>
  <Tabs.Tab title="Go">
    <Snippet
      src={snippets.go.guides.document_processing.main.step_04_run_worker}
    />
  </Tabs.Tab>
  <Tabs.Tab title="Ruby">
    <Snippet
      src={snippets.ruby.guides.document_processing.worker.step_04_run_worker}
    />
  </Tabs.Tab>
</UniversalTabs>

</Steps>

<Callout type="warning">
  When fanning out to many documents, ensure your workers have enough slots or
  use [Concurrency Control](/concepts/concurrency) to limit how many run
  simultaneously.
</Callout>

## Common Patterns

| Pattern                     | Description                                                                                                           |
| --------------------------- | --------------------------------------------------------------------------------------------------------------------- |
| **Invoice extraction**      | Parse invoices, extract line items and totals with LLM, validate amounts, post to ERP                                 |
| **Contract analysis**       | Extract clauses and terms, classify risk, route for legal review                                                      |
| **Resume parsing**          | Parse resumes, extract skills and experience, match to job requisitions                                               |
| **Form processing**         | Extract form fields from scans, validate against schemas, submit to backend systems                                   |
| **Document classification** | Classify documents by type, route to appropriate [DAG workflows](/concepts/durable-workflows/directed-acyclic-graphs) |

## Related Patterns

<Cards>
  <Cards.Card
    title="DAG Workflows"
    href="/concepts/durable-workflows/directed-acyclic-graphs"
  >
    The fixed-stage DAG pattern that document pipelines are built on.
  </Cards.Card>
  <Cards.Card title="RAG & Data Indexing" href="/guides/rag-and-indexing">
    When your goal is retrieval (chunk, embed, index) rather than extract and
    transform.
  </Cards.Card>
  <Cards.Card title="Batch Processing" href="/guides/batch-processing">
    General-purpose batch patterns that apply to document workloads.
  </Cards.Card>
  <Cards.Card
    title="Child Spawning"
    href="/concepts/durable-workflows/directed-acyclic-graphs/child-spawning"
  >
    Parallelize document processing across your worker fleet.
  </Cards.Card>
</Cards>

## Next Steps

- [DAG Workflows](/concepts/durable-workflows/directed-acyclic-graphs): define multi-stage pipelines with task dependencies
- [Rate Limits](/concepts/rate-limits): configure rate limiting for OCR and LLM APIs
- [Child Spawning](/concepts/durable-workflows/directed-acyclic-graphs/child-spawning): fan out to per-document tasks
- [Webhooks](/concepts/webhooks): trigger pipelines from file upload endpoints
- [Concurrency Control](/concepts/concurrency): limit parallel document processing
