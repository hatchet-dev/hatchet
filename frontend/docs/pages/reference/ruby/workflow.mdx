# Class Hatchet::Workflow

**Inherits:** `Object`

Represents a workflow definition with one or more tasks arranged in a DAG.

**Example: Define a simple workflow**

```ruby
wf = hatchet.workflow(name: "MyWorkflow")
step1 = wf.task(:step1) { |input, ctx| { "value" => 42 } }
wf.task(:step2, parents: [step1]) { |input, ctx|
  { "result" => ctx.task_output(step1)["value"] + 1 }
}
```

## Attributes

## `client`

_Read-only attribute_

- **Returns** [Hatchet::Client, nil] The Hatchet client

## `concurrency`

_Read-only attribute_

- **Returns** [Array\<ConcurrencyExpression>, ConcurrencyExpression, nil] Workflow-level concurrency

## `default_filters`

_Read-only attribute_

- **Returns** [Array\<DefaultFilter>] Default filters for event triggers

## `default_priority`

_Read-only attribute_

- **Returns** [Integer, nil] Default priority for runs (1-4)

## `id`

_Read-write attribute_

Get the workflow ID (UUID). If not already set, lazily resolves it by looking
up the workflow by name via the REST API.

- **Returns** [String, nil] The workflow UUID

## `idempotency`

_Read-only attribute_

- **Returns** [Hatchet::TTLBasedIdempotencyConfig, Hatchet::StatusBasedIdempotencyConfig, nil] Idempotency configuration

## `name`

_Read-only attribute_

- **Returns** [String] Workflow name

## `on_crons`

_Read-only attribute_

- **Returns** [Array\<String>] Cron expressions that trigger this workflow

## `on_events`

_Read-only attribute_

- **Returns** [Array\<String>] Event keys that trigger this workflow

## `on_failure`

_Read-only attribute_

- **Returns** [Task, nil] The on_failure task

## `on_success`

_Read-only attribute_

- **Returns** [Task, nil] The on_success task

## `sticky`

_Read-only attribute_

- **Returns** [Symbol, nil] Sticky strategy (:soft, :hard)

## `task_defaults`

_Read-only attribute_

- **Returns** [Hash, nil] Default task settings

## `tasks`

_Read-only attribute_

- **Returns** [Hash\<Symbol, Task>] Map of task name to Task object

## Public Instance Methods

## `batch_task`

`batch_task(name, batch:, **opts)`

Define a batch task within this workflow.

Batch tasks buffer concurrent runs until Hatchet flushes the batch (size
reached or flush interval), then invoke the block once with all buffered
inputs keyed by each run's task-run external id. The block must return a Hash
mapping each id to its output, or use `broadcast_output` on the batch config
to return the same result to all callers. retries is always forced to 0 for
batch tasks.

Preview: batch tasks are in beta and may change in future releases.

**Parameters:**

| Name    | Type                       | Description                              |
| ------- | -------------------------- | ---------------------------------------- |
| `name`  | `Symbol, String`           | Task name                                |
| `batch` | `Hatchet::BatchTaskConfig` | Batch configuration                      |
| `opts`  | `Hash`                     | Other Task options forwarded to `#task`. |

- **Returns** [Task] The created batch task
- **Yields** [inputs, ctx] The batch execution block, receiving a Hash of task-run external id => input

## `create_bulk_run_item`

`create_bulk_run_item(input: = {}, key: = nil, options: = nil)`

Create a bulk run item for use with run_many

**Parameters:**

| Name      | Type                          | Description       |
| --------- | ----------------------------- | ----------------- |
| `input`   | `Hash`                        | Input data        |
| `key`     | `String, nil`                 | Deduplication key |
| `options` | `TriggerWorkflowOptions, nil` | Trigger options   |

- **Returns** [Hash] Bulk run item

## `create_cron`

`create_cron(cron_name, expression, input: = {})`

Create a cron trigger for this workflow

**Parameters:**

| Name         | Type     | Description       |
| ------------ | -------- | ----------------- |
| `cron_name`  | `String` | Name for the cron |
| `expression` | `String` | Cron expression   |
| `input`      | `Hash`   | Workflow input    |

- **Raises** [Error]
- **Returns** [Object] Cron result

## `durable_task`

`durable_task(name, eviction_policy: = Hatchet::DEFAULT_DURABLE_TASK_EVICTION_POLICY, **opts)`

Define a durable task within this workflow.

**Parameters:**

| Name              | Type                           | Description              |
| ----------------- | ------------------------------ | ------------------------ |
| `name`            | `Symbol, String`               | Task name                |
| `eviction_policy` | `Hatchet::EvictionPolicy, nil` | Eviction policy for this |

durable task. Defaults to `Hatchet::DEFAULT_DURABLE_TASK_EVICTION_POLICY`
(15-minute TTL, capacity-eviction enabled). Pass `nil` to disable
eviction entirely for this task.

**Parameters:**

| Name   | Type   | Description                              |
| ------ | ------ | ---------------------------------------- |
| `opts` | `Hash` | Other Task options forwarded to `#task`. |

- **Returns** [Task] The created durable task
- **Yields** [input, ctx] The task execution block

## `initialize`

`initialize(name:, on_events: = [], on_crons: = [], concurrency: = nil, default_priority: = nil, task_defaults: = nil, default_filters: = [], sticky: = nil, idempotency: = nil, client: = nil)`

**Parameters:**

| Name               | Type                                                                             | Description               |
| ------------------ | -------------------------------------------------------------------------------- | ------------------------- |
| `name`             | `String`                                                                         | Workflow name             |
| `on_events`        | `Array<String>`                                                                  | Event trigger keys        |
| `on_crons`         | `Array<String>`                                                                  | Cron trigger expressions  |
| `concurrency`      | `Array<ConcurrencyExpression>, ConcurrencyExpression, nil`                       |                           |
| `default_priority` | `Integer, nil`                                                                   | Default priority          |
| `task_defaults`    | `Hash, nil`                                                                      | Default task settings     |
| `default_filters`  | `Array<DefaultFilter>`                                                           | Default filters           |
| `sticky`           | `Symbol, nil`                                                                    | Sticky strategy           |
| `idempotency`      | `Hatchet::TTLBasedIdempotencyConfig, Hatchet::StatusBasedIdempotencyConfig, nil` | Idempotency configuration |
| `client`           | `Hatchet::Client, nil`                                                           | The client                |

- **Returns** [Workflow] a new instance of Workflow

## `on_failure_task`

`on_failure_task(**opts)`

Define an on_failure task for this workflow

**Parameters:**

| Name   | Type   | Description  |
| ------ | ------ | ------------ |
| `opts` | `Hash` | Task options |

- **Returns** [Task]
- **Yields** [input, ctx] The on_failure task block

## `on_success_task`

`on_success_task(**opts)`

Define an on_success task for this workflow

**Parameters:**

| Name   | Type   | Description  |
| ------ | ------ | ------------ |
| `opts` | `Hash` | Task options |

- **Returns** [Task]
- **Yields** [input, ctx] The on_success task block

## `run`

`run(input = {}, options: = nil)`

Run this workflow synchronously

**Parameters:**

| Name      | Type                          | Description     |
| --------- | ----------------------------- | --------------- |
| `input`   | `Hash`                        | Workflow input  |
| `options` | `TriggerWorkflowOptions, nil` | Trigger options |

- **Raises** [Error]
- **Returns** [Hash] The workflow run output

## `run_many`

`run_many(items, return_exceptions: = false)`

Run many instances of this workflow in bulk

**Parameters:**

| Name                | Type          | Description                          |
| ------------------- | ------------- | ------------------------------------ |
| `items`             | `Array<Hash>` | Bulk run items                       |
| `return_exceptions` | `Boolean`     | Return exceptions instead of raising |

- **Raises** [Error]
- **Returns** [Array] Results

## `run_many_no_wait`

`run_many_no_wait(items)`

Run many instances without waiting for results

**Parameters:**

| Name    | Type          | Description    |
| ------- | ------------- | -------------- |
| `items` | `Array<Hash>` | Bulk run items |

- **Raises** [Error]
- **Returns** [Array\<WorkflowRunRef>]

## `run_no_wait`

`run_no_wait(input = {}, options: = nil)`

Run this workflow without waiting for the result

**Parameters:**

| Name      | Type                          | Description     |
| --------- | ----------------------------- | --------------- |
| `input`   | `Hash`                        | Workflow input  |
| `options` | `TriggerWorkflowOptions, nil` | Trigger options |

- **Raises** [Error]
- **Returns** [WorkflowRunRef]

## `schedule`

`schedule(time, input: = {}, options: = nil)`

Schedule this workflow for future execution

**Parameters:**

| Name      | Type                                  | Description      |
| --------- | ------------------------------------- | ---------------- |
| `time`    | `Time`                                | When to execute  |
| `input`   | `Hash`                                | Workflow input   |
| `options` | `ScheduleTriggerWorkflowOptions, nil` | Schedule options |

- **Raises** [Error]
- **Returns** [Object] Schedule result

## `task`

`task(name, **opts)`

Define a task within this workflow

**Parameters:**

| Name   | Type             | Description                                                 |
| ------ | ---------------- | ----------------------------------------------------------- |
| `name` | `Symbol, String` | Task name                                                   |
| `opts` | `Hash`           | Task options (parents:, execution_timeout:, retries:, etc.) |

- **Returns** [Task] The created task
- **Yields** [input, ctx] The task execution block

## `to_proto`

`to_proto(config)`

Convert this workflow to a V1::CreateWorkflowVersionRequest protobuf message.

**Parameters:**

| Name     | Type              | Description                                 |
| -------- | ----------------- | ------------------------------------------- |
| `config` | `Hatchet::Config` | The Hatchet configuration (for namespacing) |

- **Returns** [V1::CreateWorkflowVersionRequest]
