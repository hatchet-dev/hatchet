import { Callout, Card, Cards, Steps, Tabs } from "nextra/components";
import UniversalTabs from "@/components/UniversalTabs";

#### Clone a Quickstart Project

```bash copy
git clone https://github.com/hatchet-dev/hatchet-typescript-quickstart.git
```

#### CD into the project

```bash copy
cd hatchet-typescript-quickstart
```

#### Install dependencies

<UniversalTabs items={["npm", "pnpm", "yarn"]} optionKey="packageManager">
  <Tabs.Tab title="npm">```bash copy npm install ```</Tabs.Tab>
  <Tabs.Tab title="pnpm">```bash copy pnpm install ```</Tabs.Tab>
  <Tabs.Tab title="yarn">```bash copy yarn install ```</Tabs.Tab>
</UniversalTabs>
