import { snippets } from "@/lib/generated/snippets";
import { Snippet } from "@/components/code";
import { Callout, Tabs } from "nextra/components";
import UniversalTabs from "@/components/UniversalTabs";
import WorkflowDiagram from "@/components/WorkflowDiagram";
import PipelineDiagram from "@/components/PipelineDiagram";

# DAGs as Durable Workflows

A directed acyclic graph (DAG) is a workflow where every task, along with the dependencies between them, is declared upfront, before the workflow runs. In Hatchet, you define a DAG by creating a workflow, adding tasks to it, and then declaring which tasks depend on which other tasks as their parents. At runtime, Hatchet schedules the tasks in the right order, runs tasks that don't depend on each other in parallel, and passes the outputs of parent tasks to their children automatically.

<WorkflowDiagram />

In Hatchet, DAGs are a form of [durable execution](/v1/durable-execution) by definition. Every time a task in a DAG completes, Hatchet writes its output to the durable event log as a checkpoint. If the workflow is interrupted partway through, or if a later task needs to retry, Hatchet will not re-run any task that has already finished successfully — it just picks up from the checkpoints it already has. This gives DAGs the same exactly-once-style guarantees you'd get from a [durable task](/v1/durable-tasks), without having to write any explicit durable execution code yourself.

The nice thing about DAGs is that those durable properties come bundled with a set of workflow-building features you'd otherwise have to construct by hand. For instance, you get automatic parallelism between tasks that don't depend on each other, typed inputs and outputs that flow from parents to children, [parent-driven branching](#branching-with-parent-conditions), [or groups](#waiting-on-conditions-with-or-groups) for expressing complex wait conditions over sleeps and events, and a clear visual representation of the workflow in the Hatchet dashboard. In general, if the shape of the work is known before the workflow starts, a DAG is probably the most ergonomic way to get durable execution out of Hatchet.

DAGs are most useful when you know the shape of the work up front. Common examples are ETL pipelines, document processing pipelines, and CI/CD-style workflows. If you don't know the shape of the work up front — for instance, if the number of children you need to spawn depends on a runtime input, or if you need to loop until some condition is satisfied — you should reach for [durable tasks](/v1/durable-tasks) instead.

## Defining a workflow

You start by declaring a workflow with a name. The workflow object also accepts additional configuration options (retry policies, timeouts, concurrency settings, and so on), which are covered in the [tasks documentation](/v1/tasks#configuration).

<UniversalTabs items={["Python", "Typescript", "Go", "Ruby"]}>
  <Tabs.Tab title="Python">

<Snippet src={snippets.python.dag.worker.define_a_dag} />

</Tabs.Tab>
<Tabs.Tab title="Typescript">

<Snippet src={snippets.typescript.dag.workflow.declaring_a_dag_workflow} />

</Tabs.Tab>
<Tabs.Tab title="Go">

<Snippet src={snippets.go.dag.main.declaring_a_workflow} />

</Tabs.Tab>
  <Tabs.Tab title="Ruby">
    <Snippet src={snippets.ruby.dag.worker.define_a_dag} />
  </Tabs.Tab>
</UniversalTabs>

## Defining a task

Once you have a workflow, you can add tasks to it. Each task is a function that receives the workflow's input and optionally returns some output. As with standalone tasks, every task in a DAG can declare its own retries, timeouts, concurrency settings, and other configuration. For more details on how to define tasks, see the [tasks documentation](/v1/tasks).

<UniversalTabs items={["Python", "Typescript", "Go", "Ruby"]} variant="hidden">
<Tabs.Tab title="Python">

<Snippet src={snippets.python.dag.worker.first_task} />

</Tabs.Tab>
<Tabs.Tab title="Typescript">

<Snippet src={snippets.typescript.dag.workflow.first_task} />

</Tabs.Tab>
<Tabs.Tab title="Go">

<Snippet src={snippets.go.dag.main.defining_a_task} />

</Tabs.Tab>
  <Tabs.Tab title="Ruby">
    <Snippet src={snippets.ruby.dag.worker.first_task} />
  </Tabs.Tab>
</UniversalTabs>

## Adding task dependencies

The real power of a DAG comes from connecting tasks. A task can declare one or more **parent** tasks — those parents must complete successfully before this task starts, and any tasks that don't have a dependency relationship between them are run in parallel. When a task runs, its parents' outputs are available on the context object, so data flows naturally from one stage of the DAG to the next.

<UniversalTabs items={["Python", "Typescript", "Go", "Ruby"]} variant="hidden">
<Tabs.Tab title="Python">

<Snippet src={snippets.python.dag.worker.task_with_parents} />

</Tabs.Tab>
<Tabs.Tab title="Typescript">

<Snippet src={snippets.typescript.dag.workflow.second_task_with_parent} />

</Tabs.Tab>
<Tabs.Tab title="Go">

<Snippet src={snippets.go.dag.main.adding_a_task_with_a_parent} />

</Tabs.Tab>
  <Tabs.Tab title="Ruby">
    <Snippet src={snippets.ruby.dag.worker.task_with_parents} />
  </Tabs.Tab>
</UniversalTabs>

## Running a workflow

Once you've defined a workflow and registered it on a [worker](/v1/workers), you can trigger it the same way you'd trigger a standalone task — run and wait for a result, enqueue it and let it run in the background, schedule it for the future, and so on. See the [Running Tasks](/v1/running-your-task) documentation for the full set of options.

## Branching with parent conditions

Even though a DAG's structure is fixed at definition time, individual tasks can decide whether to run based on data produced earlier in the workflow. **Parent conditions** let a task inspect a parent's output and either skip or cancel itself based on what it finds. There are two operators available:

- **`skip_if`** — skip this task if the parent's output matches the condition. Downstream tasks can [check whether a parent was skipped](#checking-if-a-parent-was-skipped) and react accordingly.
- **`cancel_if`** — cancel this task if the parent's output matches the condition.

<Callout type="warning">
  A task cancelled by `cancel_if` behaves like any other cancellation in
  Hatchet, meaning its downstream dependents will be cancelled as well.
</Callout>

The most common use for parent conditions is to express sibling branches in a DAG. You declare a base task that returns some data, then define two sibling tasks with complementary `skip_if` conditions so that only one of the two runs per workflow execution.

First, declare the base task that returns a value:

<UniversalTabs items={["Python", "Typescript", "Go", "Ruby"]}>
  <Tabs.Tab title="Python">
    <Snippet src={snippets.python.conditions.worker.add_base_task} />
  </Tabs.Tab>
  <Tabs.Tab title="Typescript">
    <Snippet
      src={
        snippets.typescript.dag_match_condition.complex_workflow.add_base_task
      }
    />
  </Tabs.Tab>
  <Tabs.Tab title="Go">
    <Snippet src={snippets.go.conditions.main.add_base_task} />
  </Tabs.Tab>
  <Tabs.Tab title="Ruby">
    <Snippet src={snippets.ruby.conditions.worker.add_base_task} />
  </Tabs.Tab>
</UniversalTabs>

Then add two branches that use a parent condition with `skip_if` to decide whether to run, based on the output of the base task:

<UniversalTabs items={["Python", "Typescript", "Go", "Ruby"]} variant="hidden">
  <Tabs.Tab title="Python">
    <Snippet src={snippets.python.conditions.worker.add_branching} />
  </Tabs.Tab>
  <Tabs.Tab title="Typescript">
    <Snippet
      src={
        snippets.typescript.dag_match_condition.complex_workflow.add_branching
      }
    />
  </Tabs.Tab>
  <Tabs.Tab title="Go">
    <Snippet src={snippets.go.conditions.main.add_branching} />
  </Tabs.Tab>
  <Tabs.Tab title="Ruby">
    <Snippet src={snippets.ruby.conditions.worker.add_branching} />
  </Tabs.Tab>
</UniversalTabs>

These two tasks check whether the output of the base task was greater or less than `50`, respectively. Only one of the two will run per workflow execution, and the other will be skipped.

### Checking if a parent was skipped

When both branches of a parent condition feed into a common downstream task, that downstream task often needs to know which of the two branches actually ran. You can check whether a parent was skipped using `ctx.was_skipped`:

<UniversalTabs items={["Python", "Typescript", "Go", "Ruby"]} variant="hidden">
  <Tabs.Tab title="Python">
    <Snippet src={snippets.python.conditions.worker.add_sum} />
  </Tabs.Tab>
  <Tabs.Tab title="Typescript">
    <Snippet
      src={snippets.typescript.dag_match_condition.complex_workflow.add_sum}
    />
  </Tabs.Tab>
  <Tabs.Tab title="Go">
    <Snippet src={snippets.go.conditions.main.add_sum} />
  </Tabs.Tab>
  <Tabs.Tab title="Ruby">
    <Snippet src={snippets.ruby.conditions.worker.add_sum} />
  </Tabs.Tab>
</UniversalTabs>

## Waiting on conditions with or groups

In addition to parent conditions, a task in a DAG can wait for external signals before it runs: a sleep timer to expire, a [user event](/v1/events) to arrive, or some combination of these alongside parent conditions. You compose these together using **or groups**.

An or group is a set of conditions combined with an `Or` operator, which evaluates to `True` if **at least one** of its conditions is satisfied. Multiple or groups on the same task are combined with `AND`, so every group must have at least one satisfied condition for the task to proceed. Taken together, this lets you express arbitrarily complex sets of conditions in [conjunctive normal form](https://en.wikipedia.org/wiki/Conjunctive_normal_form) (CNF).

### Sleep + event example

The most common combination is a sleep condition alongside an event condition: proceed when an external signal arrives _or_ after a timeout, whichever comes first. This is a natural fit for human-in-the-loop workflows where you want a deadline on how long you'll wait for a response.

<UniversalTabs items={["Python", "Typescript", "Go", "Ruby"]}>
  <Tabs.Tab title="Python">
    <Snippet src={snippets.python.conditions.worker.add_wait_for_event} />
  </Tabs.Tab>
  <Tabs.Tab title="Typescript">
    <Snippet
      src={
        snippets.typescript.dag_match_condition.complex_workflow
          .add_wait_for_event
      }
    />
  </Tabs.Tab>
  <Tabs.Tab title="Go">
    <Snippet src={snippets.go.conditions.main.add_wait_for_event} />
  </Tabs.Tab>
  <Tabs.Tab title="Ruby">
    <Snippet src={snippets.ruby.conditions.worker.add_wait_for_event} />
  </Tabs.Tab>
</UniversalTabs>

In each of these snippets, a single or group wraps a sleep condition and a user event condition. The task starts as soon as either the sleep expires or the event arrives.

### Combining multiple or groups

For more complex logic, you can declare multiple or groups on a single task. As an example, consider these three conditions:

- **Condition A**: A parent task's output is greater than 50
- **Condition B**: A 30 second sleep timer expires
- **Condition C**: A `payment:processed` event arrives

To proceed when `(A or B)` **and** `(A or C)` are both satisfied, you'd declare two separate or groups: one containing `A or B`, and the other containing `A or C`. The task runs once both groups are satisfied. If `A` is true, both groups pass immediately. If `A` is false, the task needs both `B` (the sleep expires) and `C` (the event arrives) before it can run.
