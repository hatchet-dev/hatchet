// Code generated by protoc-gen-connect-go. DO NOT EDIT.
//
// Source: v2/streams.proto

package v2connect

import (
	connect "connectrpc.com/connect"
	context "context"
	errors "errors"
	v2 "github.com/hatchet-dev/hatchet/internal/services/shared/proto/v2"
	http "net/http"
	strings "strings"
)

// This is a compile-time assertion to ensure that this generated file and the connect package are
// compatible. If you get a compiler error that this constant is not defined, this code was
// generated with a version of connect newer than the one compiled into your binary. You can fix the
// problem by either regenerating this code with an older version of connect or updating the connect
// version compiled into your binary.
const _ = connect.IsAtLeastVersion1_13_0

const (
	// StreamServiceName is the fully-qualified name of the StreamService service.
	StreamServiceName = "v2.StreamService"
)

// These constants are the fully-qualified names of the RPCs defined in this package. They're
// exposed at runtime as Spec.Procedure and as the final two segments of the HTTP route.
//
// Note that these are different from the fully-qualified method names used by
// google.golang.org/protobuf/reflect/protoreflect. To convert from these constants to
// reflection-formatted method names, remove the leading slash and convert the remaining slash to a
// period.
const (
	// StreamServicePutStreamEventsProcedure is the fully-qualified name of the StreamService's
	// PutStreamEvents RPC.
	StreamServicePutStreamEventsProcedure = "/v2.StreamService/PutStreamEvents"
)

// StreamServiceClient is a client for the v2.StreamService service.
type StreamServiceClient interface {
	// Bulk-first; a single stream event is a one-element batch.
	PutStreamEvents(context.Context, *v2.PutStreamEventsRequest) (*v2.PutStreamEventsResponse, error)
}

// NewStreamServiceClient constructs a client for the v2.StreamService service. By default, it uses
// the Connect protocol with the binary Protobuf Codec, asks for gzipped responses, and sends
// uncompressed requests. To use the gRPC or gRPC-Web protocols, supply the connect.WithGRPC() or
// connect.WithGRPCWeb() options.
//
// The URL supplied here should be the base URL for the Connect or gRPC server (for example,
// http://api.acme.com or https://acme.com/grpc).
func NewStreamServiceClient(httpClient connect.HTTPClient, baseURL string, opts ...connect.ClientOption) StreamServiceClient {
	baseURL = strings.TrimRight(baseURL, "/")
	streamServiceMethods := v2.File_v2_streams_proto.Services().ByName("StreamService").Methods()
	return &streamServiceClient{
		putStreamEvents: connect.NewClient[v2.PutStreamEventsRequest, v2.PutStreamEventsResponse](
			httpClient,
			baseURL+StreamServicePutStreamEventsProcedure,
			connect.WithSchema(streamServiceMethods.ByName("PutStreamEvents")),
			connect.WithClientOptions(opts...),
		),
	}
}

// streamServiceClient implements StreamServiceClient.
type streamServiceClient struct {
	putStreamEvents *connect.Client[v2.PutStreamEventsRequest, v2.PutStreamEventsResponse]
}

// PutStreamEvents calls v2.StreamService.PutStreamEvents.
func (c *streamServiceClient) PutStreamEvents(ctx context.Context, req *v2.PutStreamEventsRequest) (*v2.PutStreamEventsResponse, error) {
	response, err := c.putStreamEvents.CallUnary(ctx, connect.NewRequest(req))
	if response != nil {
		return response.Msg, err
	}
	return nil, err
}

// StreamServiceHandler is an implementation of the v2.StreamService service.
type StreamServiceHandler interface {
	// Bulk-first; a single stream event is a one-element batch.
	PutStreamEvents(context.Context, *v2.PutStreamEventsRequest) (*v2.PutStreamEventsResponse, error)
}

// NewStreamServiceHandler builds an HTTP handler from the service implementation. It returns the
// path on which to mount the handler and the handler itself.
//
// By default, handlers support the Connect, gRPC, and gRPC-Web protocols with the binary Protobuf
// and JSON codecs. They also support gzip compression.
func NewStreamServiceHandler(svc StreamServiceHandler, opts ...connect.HandlerOption) (string, http.Handler) {
	streamServiceMethods := v2.File_v2_streams_proto.Services().ByName("StreamService").Methods()
	streamServicePutStreamEventsHandler := connect.NewUnaryHandlerSimple(
		StreamServicePutStreamEventsProcedure,
		svc.PutStreamEvents,
		connect.WithSchema(streamServiceMethods.ByName("PutStreamEvents")),
		connect.WithHandlerOptions(opts...),
	)
	return "/v2.StreamService/", http.HandlerFunc(func(w http.ResponseWriter, r *http.Request) {
		switch r.URL.Path {
		case StreamServicePutStreamEventsProcedure:
			streamServicePutStreamEventsHandler.ServeHTTP(w, r)
		default:
			http.NotFound(w, r)
		}
	})
}

// UnimplementedStreamServiceHandler returns CodeUnimplemented from all methods.
type UnimplementedStreamServiceHandler struct{}

func (UnimplementedStreamServiceHandler) PutStreamEvents(context.Context, *v2.PutStreamEventsRequest) (*v2.PutStreamEventsResponse, error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("v2.StreamService.PutStreamEvents is not implemented"))
}
