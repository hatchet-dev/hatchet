// Code generated by protoc-gen-go-grpc. DO NOT EDIT.
// versions:
// - protoc-gen-go-grpc v1.2.0
// - protoc             v5.29.3
// source: v1/operator.proto

package v1

import (
	context "context"
	contracts "github.com/hatchet-dev/hatchet/internal/services/dispatcher/contracts"
	grpc "google.golang.org/grpc"
	codes "google.golang.org/grpc/codes"
	status "google.golang.org/grpc/status"
)

// This is a compile-time assertion to ensure that this generated file
// is compatible with the grpc package it is being compiled against.
// Requires gRPC-Go v1.32.0 or later.
const _ = grpc.SupportPackageIsVersion7

// OperatorServiceClient is the client API for OperatorService service.
//
// For semantics around ctx use and closing/ending streaming RPCs, please refer to https://pkg.go.dev/google.golang.org/grpc/?tab=doc#ClientConn.NewStream.
type OperatorServiceClient interface {
	// Register upserts the operator and creates or resumes the worker for this connection. It
	// does not register workflows or actions: workflows are put through AdminService.PutWorkflow
	// on the same connection, and actions are added on the Listen stream.
	Register(ctx context.Context, in *OperatorRegisterRequest, opts ...grpc.CallOption) (*OperatorRegisterResponse, error)
	// Listen activates a registered worker for the lifetime of the stream, streams assigned
	// actions to it, and accepts heartbeats and action set deltas from it.
	//
	// Protocol:
	//  1. The first client message MUST be start, naming the worker returned by Register. Any
	//     other first message is rejected with InvalidArgument, and a second start on the same
	//     stream is also rejected. The server waits at most 30 seconds for it.
	//  2. Every server message is an OperatorListenResponse: either an assigned action from the
	//     dispatcher fan-out or an ack for an actions delta.
	//  3. The client sends a heartbeat every 4 seconds on this stream. A worker whose heartbeat
	//     goes stale is treated as inactive by the scheduler. See OperatorHeartbeat for why the
	//     heartbeat is in-stream rather than a separate RPC like an SDK worker's.
	//  4. The client sends actions deltas whenever the set of actions it can run changes. Deltas
	//     are applied incrementally to the worker's action set and never replace it; a removed
	//     action stops being assigned to the worker within about a second. Each delta carries at
	//     most 1000 ids (adds plus removes); larger deltas are rejected with InvalidArgument.
	//  5. Delta acknowledgement. Each delta carries a sequence number that is positive and
	//     strictly increasing on the stream. The server applies deltas in stream order and
	//     answers each one with an OperatorActionsAck carrying its sequence once the change is
	//     committed. An ack for sequence N therefore also confirms every lower sequence. A delta
	//     whose sequence is 0 is applied but never acknowledged. The server ends the stream
	//     instead of acknowledging when a delta cannot be applied or the ack cannot be sent.
	//  6. Recovery. The client keeps every sent delta until its ack arrives. After a reconnect
	//     it first resends the unacknowledged deltas in order; if the reconnect did not resume
	//     the worker (OperatorRegisterResponse.resumed is false) it instead resends its whole
	//     desired action set, since a new worker starts with no actions. Applying a delta twice
	//     is harmless: adding an action the worker has and removing one it lacks are no-ops, so
	//     a delta that was committed but not acknowledged converges on replay.
	//  7. When the stream ends (client close, network failure, or engine shutdown) the worker is
	//     deactivated. Reconnect by calling Register with worker_id set, then Listen again; the
	//     resumed worker keeps its action set.
	//
	// Requires the hatchet-operator-id metadata.
	Listen(ctx context.Context, opts ...grpc.CallOption) (OperatorService_ListenClient, error)
	// PauseWorker stops the scheduler assigning to one of the operator's workers, or lets it be
	// assigned to again with paused = false. It returns once the change is committed, so an
	// operator that pauses before draining knows no further work will arrive, and it works while
	// the Listen stream is being torn down or reconnected. Register clears the pause when it
	// resumes a worker, so an operator that crashed while paused comes back assignable.
	// Requires the hatchet-operator-id metadata; the worker_id must belong to the operator.
	PauseWorker(ctx context.Context, in *OperatorPauseWorkerRequest, opts ...grpc.CallOption) (*OperatorPauseWorkerResponse, error)
	// SendStepActionEvent reports task progress (started, completed, failed) for an action that
	// was delivered on a Listen stream. Same semantics as Dispatcher.SendStepActionEvent.
	// Requires the hatchet-operator-id metadata; the event's worker_id must belong to the
	// operator.
	SendStepActionEvent(ctx context.Context, in *contracts.StepActionEvent, opts ...grpc.CallOption) (*contracts.ActionEventResponse, error)
	// DurableTask is the durable task event stream, identical to V1Dispatcher.DurableTask. The
	// first client message registers the worker id returned by Register; a worker that belongs
	// to another operator is rejected with PermissionDenied. Requires the hatchet-operator-id
	// metadata.
	DurableTask(ctx context.Context, opts ...grpc.CallOption) (OperatorService_DurableTaskClient, error)
}

type operatorServiceClient struct {
	cc grpc.ClientConnInterface
}

func NewOperatorServiceClient(cc grpc.ClientConnInterface) OperatorServiceClient {
	return &operatorServiceClient{cc}
}

func (c *operatorServiceClient) Register(ctx context.Context, in *OperatorRegisterRequest, opts ...grpc.CallOption) (*OperatorRegisterResponse, error) {
	out := new(OperatorRegisterResponse)
	err := c.cc.Invoke(ctx, "/v1.OperatorService/Register", in, out, opts...)
	if err != nil {
		return nil, err
	}
	return out, nil
}

func (c *operatorServiceClient) Listen(ctx context.Context, opts ...grpc.CallOption) (OperatorService_ListenClient, error) {
	stream, err := c.cc.NewStream(ctx, &OperatorService_ServiceDesc.Streams[0], "/v1.OperatorService/Listen", opts...)
	if err != nil {
		return nil, err
	}
	x := &operatorServiceListenClient{stream}
	return x, nil
}

type OperatorService_ListenClient interface {
	Send(*OperatorListenRequest) error
	Recv() (*OperatorListenResponse, error)
	grpc.ClientStream
}

type operatorServiceListenClient struct {
	grpc.ClientStream
}

func (x *operatorServiceListenClient) Send(m *OperatorListenRequest) error {
	return x.ClientStream.SendMsg(m)
}

func (x *operatorServiceListenClient) Recv() (*OperatorListenResponse, error) {
	m := new(OperatorListenResponse)
	if err := x.ClientStream.RecvMsg(m); err != nil {
		return nil, err
	}
	return m, nil
}

func (c *operatorServiceClient) PauseWorker(ctx context.Context, in *OperatorPauseWorkerRequest, opts ...grpc.CallOption) (*OperatorPauseWorkerResponse, error) {
	out := new(OperatorPauseWorkerResponse)
	err := c.cc.Invoke(ctx, "/v1.OperatorService/PauseWorker", in, out, opts...)
	if err != nil {
		return nil, err
	}
	return out, nil
}

func (c *operatorServiceClient) SendStepActionEvent(ctx context.Context, in *contracts.StepActionEvent, opts ...grpc.CallOption) (*contracts.ActionEventResponse, error) {
	out := new(contracts.ActionEventResponse)
	err := c.cc.Invoke(ctx, "/v1.OperatorService/SendStepActionEvent", in, out, opts...)
	if err != nil {
		return nil, err
	}
	return out, nil
}

func (c *operatorServiceClient) DurableTask(ctx context.Context, opts ...grpc.CallOption) (OperatorService_DurableTaskClient, error) {
	stream, err := c.cc.NewStream(ctx, &OperatorService_ServiceDesc.Streams[1], "/v1.OperatorService/DurableTask", opts...)
	if err != nil {
		return nil, err
	}
	x := &operatorServiceDurableTaskClient{stream}
	return x, nil
}

type OperatorService_DurableTaskClient interface {
	Send(*DurableTaskRequest) error
	Recv() (*DurableTaskResponse, error)
	grpc.ClientStream
}

type operatorServiceDurableTaskClient struct {
	grpc.ClientStream
}

func (x *operatorServiceDurableTaskClient) Send(m *DurableTaskRequest) error {
	return x.ClientStream.SendMsg(m)
}

func (x *operatorServiceDurableTaskClient) Recv() (*DurableTaskResponse, error) {
	m := new(DurableTaskResponse)
	if err := x.ClientStream.RecvMsg(m); err != nil {
		return nil, err
	}
	return m, nil
}

// OperatorServiceServer is the server API for OperatorService service.
// All implementations must embed UnimplementedOperatorServiceServer
// for forward compatibility
type OperatorServiceServer interface {
	// Register upserts the operator and creates or resumes the worker for this connection. It
	// does not register workflows or actions: workflows are put through AdminService.PutWorkflow
	// on the same connection, and actions are added on the Listen stream.
	Register(context.Context, *OperatorRegisterRequest) (*OperatorRegisterResponse, error)
	// Listen activates a registered worker for the lifetime of the stream, streams assigned
	// actions to it, and accepts heartbeats and action set deltas from it.
	//
	// Protocol:
	//  1. The first client message MUST be start, naming the worker returned by Register. Any
	//     other first message is rejected with InvalidArgument, and a second start on the same
	//     stream is also rejected. The server waits at most 30 seconds for it.
	//  2. Every server message is an OperatorListenResponse: either an assigned action from the
	//     dispatcher fan-out or an ack for an actions delta.
	//  3. The client sends a heartbeat every 4 seconds on this stream. A worker whose heartbeat
	//     goes stale is treated as inactive by the scheduler. See OperatorHeartbeat for why the
	//     heartbeat is in-stream rather than a separate RPC like an SDK worker's.
	//  4. The client sends actions deltas whenever the set of actions it can run changes. Deltas
	//     are applied incrementally to the worker's action set and never replace it; a removed
	//     action stops being assigned to the worker within about a second. Each delta carries at
	//     most 1000 ids (adds plus removes); larger deltas are rejected with InvalidArgument.
	//  5. Delta acknowledgement. Each delta carries a sequence number that is positive and
	//     strictly increasing on the stream. The server applies deltas in stream order and
	//     answers each one with an OperatorActionsAck carrying its sequence once the change is
	//     committed. An ack for sequence N therefore also confirms every lower sequence. A delta
	//     whose sequence is 0 is applied but never acknowledged. The server ends the stream
	//     instead of acknowledging when a delta cannot be applied or the ack cannot be sent.
	//  6. Recovery. The client keeps every sent delta until its ack arrives. After a reconnect
	//     it first resends the unacknowledged deltas in order; if the reconnect did not resume
	//     the worker (OperatorRegisterResponse.resumed is false) it instead resends its whole
	//     desired action set, since a new worker starts with no actions. Applying a delta twice
	//     is harmless: adding an action the worker has and removing one it lacks are no-ops, so
	//     a delta that was committed but not acknowledged converges on replay.
	//  7. When the stream ends (client close, network failure, or engine shutdown) the worker is
	//     deactivated. Reconnect by calling Register with worker_id set, then Listen again; the
	//     resumed worker keeps its action set.
	//
	// Requires the hatchet-operator-id metadata.
	Listen(OperatorService_ListenServer) error
	// PauseWorker stops the scheduler assigning to one of the operator's workers, or lets it be
	// assigned to again with paused = false. It returns once the change is committed, so an
	// operator that pauses before draining knows no further work will arrive, and it works while
	// the Listen stream is being torn down or reconnected. Register clears the pause when it
	// resumes a worker, so an operator that crashed while paused comes back assignable.
	// Requires the hatchet-operator-id metadata; the worker_id must belong to the operator.
	PauseWorker(context.Context, *OperatorPauseWorkerRequest) (*OperatorPauseWorkerResponse, error)
	// SendStepActionEvent reports task progress (started, completed, failed) for an action that
	// was delivered on a Listen stream. Same semantics as Dispatcher.SendStepActionEvent.
	// Requires the hatchet-operator-id metadata; the event's worker_id must belong to the
	// operator.
	SendStepActionEvent(context.Context, *contracts.StepActionEvent) (*contracts.ActionEventResponse, error)
	// DurableTask is the durable task event stream, identical to V1Dispatcher.DurableTask. The
	// first client message registers the worker id returned by Register; a worker that belongs
	// to another operator is rejected with PermissionDenied. Requires the hatchet-operator-id
	// metadata.
	DurableTask(OperatorService_DurableTaskServer) error
	mustEmbedUnimplementedOperatorServiceServer()
}

// UnimplementedOperatorServiceServer must be embedded to have forward compatible implementations.
type UnimplementedOperatorServiceServer struct {
}

func (UnimplementedOperatorServiceServer) Register(context.Context, *OperatorRegisterRequest) (*OperatorRegisterResponse, error) {
	return nil, status.Errorf(codes.Unimplemented, "method Register not implemented")
}
func (UnimplementedOperatorServiceServer) Listen(OperatorService_ListenServer) error {
	return status.Errorf(codes.Unimplemented, "method Listen not implemented")
}
func (UnimplementedOperatorServiceServer) PauseWorker(context.Context, *OperatorPauseWorkerRequest) (*OperatorPauseWorkerResponse, error) {
	return nil, status.Errorf(codes.Unimplemented, "method PauseWorker not implemented")
}
func (UnimplementedOperatorServiceServer) SendStepActionEvent(context.Context, *contracts.StepActionEvent) (*contracts.ActionEventResponse, error) {
	return nil, status.Errorf(codes.Unimplemented, "method SendStepActionEvent not implemented")
}
func (UnimplementedOperatorServiceServer) DurableTask(OperatorService_DurableTaskServer) error {
	return status.Errorf(codes.Unimplemented, "method DurableTask not implemented")
}
func (UnimplementedOperatorServiceServer) mustEmbedUnimplementedOperatorServiceServer() {}

// UnsafeOperatorServiceServer may be embedded to opt out of forward compatibility for this service.
// Use of this interface is not recommended, as added methods to OperatorServiceServer will
// result in compilation errors.
type UnsafeOperatorServiceServer interface {
	mustEmbedUnimplementedOperatorServiceServer()
}

func RegisterOperatorServiceServer(s grpc.ServiceRegistrar, srv OperatorServiceServer) {
	s.RegisterService(&OperatorService_ServiceDesc, srv)
}

func _OperatorService_Register_Handler(srv interface{}, ctx context.Context, dec func(interface{}) error, interceptor grpc.UnaryServerInterceptor) (interface{}, error) {
	in := new(OperatorRegisterRequest)
	if err := dec(in); err != nil {
		return nil, err
	}
	if interceptor == nil {
		return srv.(OperatorServiceServer).Register(ctx, in)
	}
	info := &grpc.UnaryServerInfo{
		Server:     srv,
		FullMethod: "/v1.OperatorService/Register",
	}
	handler := func(ctx context.Context, req interface{}) (interface{}, error) {
		return srv.(OperatorServiceServer).Register(ctx, req.(*OperatorRegisterRequest))
	}
	return interceptor(ctx, in, info, handler)
}

func _OperatorService_Listen_Handler(srv interface{}, stream grpc.ServerStream) error {
	return srv.(OperatorServiceServer).Listen(&operatorServiceListenServer{stream})
}

type OperatorService_ListenServer interface {
	Send(*OperatorListenResponse) error
	Recv() (*OperatorListenRequest, error)
	grpc.ServerStream
}

type operatorServiceListenServer struct {
	grpc.ServerStream
}

func (x *operatorServiceListenServer) Send(m *OperatorListenResponse) error {
	return x.ServerStream.SendMsg(m)
}

func (x *operatorServiceListenServer) Recv() (*OperatorListenRequest, error) {
	m := new(OperatorListenRequest)
	if err := x.ServerStream.RecvMsg(m); err != nil {
		return nil, err
	}
	return m, nil
}

func _OperatorService_PauseWorker_Handler(srv interface{}, ctx context.Context, dec func(interface{}) error, interceptor grpc.UnaryServerInterceptor) (interface{}, error) {
	in := new(OperatorPauseWorkerRequest)
	if err := dec(in); err != nil {
		return nil, err
	}
	if interceptor == nil {
		return srv.(OperatorServiceServer).PauseWorker(ctx, in)
	}
	info := &grpc.UnaryServerInfo{
		Server:     srv,
		FullMethod: "/v1.OperatorService/PauseWorker",
	}
	handler := func(ctx context.Context, req interface{}) (interface{}, error) {
		return srv.(OperatorServiceServer).PauseWorker(ctx, req.(*OperatorPauseWorkerRequest))
	}
	return interceptor(ctx, in, info, handler)
}

func _OperatorService_SendStepActionEvent_Handler(srv interface{}, ctx context.Context, dec func(interface{}) error, interceptor grpc.UnaryServerInterceptor) (interface{}, error) {
	in := new(contracts.StepActionEvent)
	if err := dec(in); err != nil {
		return nil, err
	}
	if interceptor == nil {
		return srv.(OperatorServiceServer).SendStepActionEvent(ctx, in)
	}
	info := &grpc.UnaryServerInfo{
		Server:     srv,
		FullMethod: "/v1.OperatorService/SendStepActionEvent",
	}
	handler := func(ctx context.Context, req interface{}) (interface{}, error) {
		return srv.(OperatorServiceServer).SendStepActionEvent(ctx, req.(*contracts.StepActionEvent))
	}
	return interceptor(ctx, in, info, handler)
}

func _OperatorService_DurableTask_Handler(srv interface{}, stream grpc.ServerStream) error {
	return srv.(OperatorServiceServer).DurableTask(&operatorServiceDurableTaskServer{stream})
}

type OperatorService_DurableTaskServer interface {
	Send(*DurableTaskResponse) error
	Recv() (*DurableTaskRequest, error)
	grpc.ServerStream
}

type operatorServiceDurableTaskServer struct {
	grpc.ServerStream
}

func (x *operatorServiceDurableTaskServer) Send(m *DurableTaskResponse) error {
	return x.ServerStream.SendMsg(m)
}

func (x *operatorServiceDurableTaskServer) Recv() (*DurableTaskRequest, error) {
	m := new(DurableTaskRequest)
	if err := x.ServerStream.RecvMsg(m); err != nil {
		return nil, err
	}
	return m, nil
}

// OperatorService_ServiceDesc is the grpc.ServiceDesc for OperatorService service.
// It's only intended for direct use with grpc.RegisterService,
// and not to be introspected or modified (even as a copy)
var OperatorService_ServiceDesc = grpc.ServiceDesc{
	ServiceName: "v1.OperatorService",
	HandlerType: (*OperatorServiceServer)(nil),
	Methods: []grpc.MethodDesc{
		{
			MethodName: "Register",
			Handler:    _OperatorService_Register_Handler,
		},
		{
			MethodName: "PauseWorker",
			Handler:    _OperatorService_PauseWorker_Handler,
		},
		{
			MethodName: "SendStepActionEvent",
			Handler:    _OperatorService_SendStepActionEvent_Handler,
		},
	},
	Streams: []grpc.StreamDesc{
		{
			StreamName:    "Listen",
			Handler:       _OperatorService_Listen_Handler,
			ServerStreams: true,
			ClientStreams: true,
		},
		{
			StreamName:    "DurableTask",
			Handler:       _OperatorService_DurableTask_Handler,
			ServerStreams: true,
			ClientStreams: true,
		},
	},
	Metadata: "v1/operator.proto",
}
