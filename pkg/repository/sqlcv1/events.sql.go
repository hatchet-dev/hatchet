// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.29.0
// source: events.sql

package sqlcv1

import (
	"context"

	"github.com/google/uuid"
)

const getEvent = `-- name: GetEvent :one
SELECT e.id, e.seen_at, e.tenant_id, e.external_id, e.key, e.additional_metadata, e.scope, e.triggering_webhook_name
FROM v1_event_lookup_table elt
JOIN v1_event e ON (elt.event_id, elt.event_seen_at, elt.tenant_id) = (e.id, e.seen_at, e.tenant_id)
WHERE
    elt.external_id = $1
`

func (q *Queries) GetEvent(ctx context.Context, db DBTX, externalID uuid.UUID) (*V1Event, error) {
	row := db.QueryRow(ctx, getEvent, externalID)
	var i V1Event
	err := row.Scan(
		&i.ID,
		&i.SeenAt,
		&i.TenantID,
		&i.ExternalID,
		&i.Key,
		&i.AdditionalMetadata,
		&i.Scope,
		&i.TriggeringWebhookName,
	)
	return &i, err
}
